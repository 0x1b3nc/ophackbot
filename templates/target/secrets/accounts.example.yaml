# Copy to accounts.yaml and fill in. accounts.yaml is gitignored.
# Never commit live passwords.

accounts:
  A:
    username: "user_a@example.com"
    password: "ChangeMe-A"
    role: "user"
  B:
    username: "user_b@example.com"
    password: "ChangeMe-B"
    role: "user"

login:
  path: "/login"
  # method: POST
  # user_field: username
  # pass_field: password
  # csrf_field: csrf_token
