# Workspace State

Public/demo state only.

I keep private program state, accounts, report status, session notes, and
target-specific evidence in a private workspace or under `targets/<program>/`
files that I don't commit to the public repo.

## Public kit status (2026-07-20)

- CLI scaffold is in: policy guard, knowledge router, planner, evidence/redaction,
  runners (print-first / `--approve`), report drafts
- Demo target: `targets/demo` with template scope (`example.com`)
- Tests: `python -m unittest discover -s tests -v`
